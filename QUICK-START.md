# 🚀 ONE-COMMAND DEPLOY

Copy and paste this into your terminal:

```bash
cd "/Users/abish/Desktop/GITHUB PROJECTS/network-monitoring-app 2" && ./deploy-interactive.sh
```

OR for instant Vercel deploy:

```bash
cd "/Users/abish/Desktop/GITHUB PROJECTS/network-monitoring-app 2" && npx vercel --prod
```

That's it! Your app will be live in 2 minutes! 🎉
